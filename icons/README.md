Положите сюда `tor_router_apply.svg`, если хотите свою иконку для .desktop-ярлыка
(создаётся командой `--install`). Если файла нет — используется системная иконка
`security-high` по умолчанию, ничего не ломается.
